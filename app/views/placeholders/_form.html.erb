<%= form_for(@placeholder) do |f| %>
  <% if @placeholder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@placeholder.errors.count, "error") %> prohibited this placeholder from being saved:</h2>

      <ul>
      <% @placeholder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
